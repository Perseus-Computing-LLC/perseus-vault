# Smithery.ai configuration
startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      dbPath:
        type: string
        description: Path to the SQLite database file
        default: ~/.perseus-vault/data/perseus-vault.db
  commandFunction: |-
    (config) => ({
      command: 'perseus-vault',
      args: ['serve', '--db', config.dbPath || '~/.perseus-vault/data/perseus-vault.db']
    })
  exampleConfig:
    dbPath: ~/.perseus-vault/data/perseus-vault.db
